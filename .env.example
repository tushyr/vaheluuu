# Use a unique value of at least 12 characters. Never commit the real secret.
P23_ADMIN_SECRET=replace-with-a-long-random-secret
# Use a managed PostgreSQL connection locally. Vercel's Neon integration provides
# this variable automatically for connected Preview and Production deployments.
DATABASE_URL=postgresql://user:password@host:5432/project23?sslmode=require
